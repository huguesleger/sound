{% extends ':back:templateAdmin.html.twig' %}

{#  {% block content %}
      
    <h1>Sectionnames list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for sectionName in sectionNames %}
            <tr>
                <td><a href="{{ path('sectionname_show', { 'id': sectionName.id }) }}">{{ sectionName.id }}</a></td>
                <td>{{ sectionName.name }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('sectionname_show', { 'id': sectionName.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('sectionname_edit', { 'id': sectionName.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('sectionname_new') }}">Create a new sectionName</a>
        </li>
    </ul>
{% endblock %}#}


{% block content %}
      
       <div class="">
           
           
           
          <div class="page-title">
            <div class="title_left">
                <h3>Nom Section</h3>
                 </div>
                <span class="btn-title-left col-md-2"><a class="btn btn-dark btn-sm" href="{{ path('sectionname_new') }}">Ajouter</a></span>
           

       </div>
          <div class="clearfix"></div>

 
          
                <div class="row">

            <div class="col-md-12 col-sm-12 col-xs-12">
              <div class="x_panel">
                <div class="x_title">
                  <h2>Mes noms de section</h2>
                  
                  <div class="clearfix"></div>
                </div>
                <div class="x_content">
                 
                  <table id="datatable" class="table table-striped projects">
                    <thead>
                      <tr>
                        <th style="width: 5%">#</th>
                        {#<th style="width: 12%">Icon</th>#}
                        <th style="width: 30%">Nom</th>
                        <th style="width: 20%">Actions</th>
                      </tr>
                    </thead>


                    <tbody>
                        {% for sectionName in sectionNames %}
                      <tr>  
                           <td> {% set counter = ( counter | default(0) ) + 1 %}
                           <small> {{ counter }}</small></td>
                       {#<td><i class="fa fa-list" aria-hidden="true"></i></td>#}
                        <td><a>{{ sectionName.name }}</a>
                        <br />
                          <small>Créer le {#{{ sound.date|date ('j/m/y à H:i:s') }}#}</small></td>
                        <td><a href="{{ path('sectionname_show', { 'id': sectionName.id }) }}" class="btn btn-primary btn-xs"><i class="fa fa-folder"></i> View </a>
                          <a href="{{ path('sectionname_edit', { 'id': sectionName.id }) }}" class="btn btn-info btn-xs"><i class="fa fa-pencil"></i> Edit </a>
                          <a href="{{ path('sectionname_delete', { 'id': sectionName.id }) }}" class="btn btn-danger btn-xs"><i class="fa fa-trash-o"></i> Delete </a></td>
                      </tr>
                       {% endfor %}
                    </tbody>
                  </table>
                </div>
              </div>
            </div>

                </div>
                    
                   </div>   
                       
                
{% endblock %}

{% block scriptForm %}
{#     <script type="text/javascript" src="{{asset('js/backend/form/form_notification.js')}}"></script>#}
      <script type="text/javascript" src="{{asset('js/backend/form.js')}}"></script>

{% endblock %}