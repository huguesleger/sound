{% extends ':back:templateAdmin.html.twig' %}

  {% block content %}
    <h1>Animationtexte</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ animationTexte.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ animationTexte.nom }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('animationtexte_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('animationtexte_edit', { 'id': animationTexte.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
