{% extends ':back:templateAdmin.html.twig' %}
{#
  {% block content %}
      
      {% for message in app.flashes('succes') %}
      <div id="success">
      </div>
      {% endfor %}
      
      {% for message in app.flashes('delete') %}
      <div id="delete">
      </div>
      {% endfor %}
      
      {% for message in app.flashes('erreur') %}
      <div id="error">
      </div>
      {% endfor %}
    <h1>Headertextes list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Texte</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for headerTexte in headerTextes %}
            <tr>
                <td><a href="{{ path('headertexte_show', { 'id': headerTexte.id }) }}">{{ headerTexte.id }}</a></td>
                <td>{{ headerTexte.texte }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('headertexte_show', { 'id': headerTexte.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('headertexte_edit', { 'id': headerTexte.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('headertexte_new') }}">Create a new headerTexte</a>
        </li>
    </ul>
{% endblock %}
{% block scriptForm %}
    <script type="text/javascript" src="{{asset('js/backend/form.js')}}"></script>
{% endblock %}#}

{% block content %}
      
       <div class="">
           
           
           
          <div class="page-title">
            <div class="title_left">
                <h3>Phrase d'accroche</h3>
                 </div>
                <span class="btn-title-left col-md-2"><a class="btn btn-dark btn-sm" href="{{ path('headertexte_new') }}">Ajouter</a></span>
     

       </div>
          <div class="clearfix"></div>

 
          
                <div class="row">

            <div class="col-md-12 col-sm-12 col-xs-12">
              <div class="x_panel">

                <div class="x_content">
                 
                  <table class="table table-striped projects">
                    <thead>
                      <tr>
                        <th style="width: 1%">#</th>
                        {#<th style="width: 12%">Icon</th>#}
                        <th style="width: 10%">Date</th>
                        <th style="width: 20%">Texte</th>
                        <th style="width: 20%">Animation</th>
                        <th style="width: 20%">Actions</th>
                      </tr>
                    </thead>


                    <tbody>
                        {% for headerTexte in headerTextes %}
                      <tr>  
                           <td> {% set counter = ( counter | default(0) ) + 1 %}
                           <small> {{ counter }}</small></td>
                       {#<td><i class="fa fa-list" aria-hidden="true"></i></td>#}
                           <td><small>Créer le {{ headerTexte.date|date ('j/m/y à H:i:s') }}</small></td>
                        <td><a>{{ headerTexte.texte }}</a></td>
                        <td>{{ headerTexte.animationTexte}}</td>
                        <td><a href="{{ path('headertexte_edit', { 'id': headerTexte.id }) }}" class="btn btn-info btn-xs"><i class="fa fa-pencil"></i> Edit </a>
                          <a href="{{ path('headertexte_delete', { 'id': headerTexte.id }) }}" class="btn btn-danger btn-xs"><i class="fa fa-trash-o"></i> Delete </a></td>
                      </tr>
                       {% endfor %}
                    </tbody>
                  </table>
                </div>
              </div>
            </div>

                </div>
                    
                   </div>   
                       
                
{% endblock %}

{% block scriptForm %}
{#     <script type="text/javascript" src="{{asset('js/backend/form/form_notification.js')}}"></script>#}
      <script type="text/javascript" src="{{asset('js/backend/form.js')}}"></script>

{% endblock %}
