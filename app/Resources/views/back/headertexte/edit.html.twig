{% extends ':back:templateAdmin.html.twig' %}

  {% block content %}
      
       {% for message in app.flashes('update') %}
      <div id="update">
      </div>
      {% endfor %}
      
      {% for message in app.flashes('erreur') %}
      <div id="error">
      </div>
      {% endfor %}
    <h1>Headertexte edit</h1>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input type="submit" value="Edit" />
    {{ form_end(edit_form) }}

    <ul>
        <li>
            <a href="{{ path('headertexte_index') }}">Back to the list</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
{% block scriptForm %}
    <script type="text/javascript" src="{{asset('js/backend/form.js')}}"></script>
{% endblock %}
