{% extends ':back:templateAdmin.html.twig' %}

  {% block content %}
{#    <h1>Imagemobile edit</h1>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        
        <input type="submit" value="Edit" />
    {{ form_end(edit_form) }}

    <ul>
        <li>
            <a href="{{ path('imagemobile_index') }}">Back to the list</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}#}

<div class="row">  
   <div class="col-md-12 col-xs-12">
              <div class="x_panel">
                <div class="x_title">
                 <h2>Image mobile edit</h2>   
                    <a class="btn btn-dark btn-xs back-list" href="{{ path('imagemobile_index') }}"><i class="fa fa-reply" aria-hidden="true"></i>Retour images</a>
                  <div class="clearfix"></div>
                </div>
                <div class="x_content">
                  <br>
                    {{ form_start(edit_form) }}
                    <div class="wrap-mobile">
                           <label for="appbundle_imagemobile_image" class="input-label">
          <i class="fa fa-upload"></i>Select fichier</label>
          {{form_row(edit_form.image)}}
           <small> * min size: 375px X 250px</small>
           <p><small>* taille max: 500 Ko</small></p>
       
                <img class="default-mobile" id="selectImage" src="{{asset('uploads/header/')~imageMobile.image}}" alt="your image" />
                    </div>
                    <br><br>
                     <div class="ln_solid"></div>
                    {{ form_row(edit_form.publier) }}

              </div>
                <div class="form-group btn-right">
                <button type="submit" class="btn btn-success"><i class="fa fa-upload" aria-hidden="true"></i>Mettre à jour</button>
                </div>
            </div>
   </div>
                           
       </div>
 {{ form_end(edit_form) }}
 
{% endblock %}
{% block scriptForm %}
<script type="text/javascript" src="{{asset('js/backend/form.js')}}"></script>
{% endblock %}