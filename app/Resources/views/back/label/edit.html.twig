{% extends ':back:templateAdmin.html.twig' %}

  {% block content %}
{#    <h1>Label edit</h1>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input type="submit" value="Edit" />
    {{ form_end(edit_form) }}

    <ul>
        <li>
            <a href="{{ path('label_index') }}">Back to the list</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>#}

 {#   <div class="x_content">
  <div class="modal fade bs-example-modal-lg" id="myModal{{ label.id }}" data-toggle="modal" tabindex="-1" role="dialog">
                  <div class="modal-dialog modal-lg">
                    <div class="modal-content">

                      <div class="modal-header">
                        <a class="btn btn-dark btn-xs back-list" data-dismiss="modal">x</a>
                        <h4 class="modal-title" id="myModalLabel">Label edit</h4>
                      </div>
                            {{ form_start(edit_form) }}
                      <div class="modal-body">
                          
                       {{ form_widget(edit_form) }}
                      </div>
                      <div class="modal-footer">
                      <button type="submit" id="RegisterEditform" class="btn btn-success">edit</button>
                      </div>
                      {{ form_end(edit_form) }}
                    </div>
                  </div>
                </div>
                      </div>#}


    <div class="row">  
   <div class="col-md-12 col-xs-12">
              <div class="x_panel">
                <div class="x_title">
                    <h2>Edit label</h2>
                    <a class="btn btn-dark btn-xs back-list" href="{{ path('label_index') }}">Back to the list</a>
                  <div class="clearfix"></div>
                </div>
                <div class="x_content">
                  <br>
                   {{ form_start(edit_form) }}
                                    
                           {{ form_row(edit_form.nom) }}
                            <p><small>* champ obligatoire</small></p>

                </div>
              </div>
                                <div class="form-group btn-right">
                        <button type="submit" class="btn btn-success">Mettre à jour</button>
                      </div>
            </div>
    </div>
     {{ form_end(edit_form) }}
 
 
 
{% endblock %}
