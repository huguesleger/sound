{% extends ':back:templateAdmin.html.twig' %}

  {% block content %}
{#    <h1>Social edit</h1>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input type="submit" value="Edit" />
    {{ form_end(edit_form) }}

    <ul>
        <li>
            <a href="{{ path('social_index') }}">Back to the list</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}#}

    <div class="row">  
   <div class="col-md-12 col-xs-12">
              <div class="x_panel">
                <div class="x_title">
                    <h2>Social edit</h2>
                    <a class="btn btn-dark btn-xs back-list" href="{{ path('social_index') }}"><i class="fa fa-reply" aria-hidden="true"></i>Retour socials</a>
                  <div class="clearfix"></div>
                </div>
                <div class="x_content">
                  <br>
                   {{ form_start(edit_form) }}
                                    
                           {{ form_row(edit_form.nom) }}
                           <p><small>* champ obligatoire</small></p>
                           {{ form_row(edit_form.url) }}
                           <p><small>* champ obligatoire</small></p>
                </div>
              </div>
                                <div class="form-group btn-right">
                        <button type="submit" class="btn btn-success"><i class="fa fa-upload" aria-hidden="true"></i>Mettre à jour</button>
                      </div>
            </div>
    </div>
     {{ form_end(edit_form) }}
{% endblock %}
