    {% extends ':back:templateAdmin.html.twig' %}

        {% block content %}

            <div class="row">  
                <div class="col-md-12 col-xs-12">
                    <div class="x_panel">
                        <div class="x_title">
                            <h2>Intro production edit </h2>
                            <a class="btn btn-dark btn-xs back-list" href="{{ path('production_index') }}"><i class="fa fa-reply" aria-hidden="true"></i>Retour productions</a>
                            <div class="clearfix"></div>
                        </div>
                            
                            <div class="x_content">
                                <br>
                                {{ form_start(edit_form) }}

                                {{ form_row(edit_form.titre) }}
                                {{ form_row(edit_form.texte) }}
                                {{ form_row(edit_form.publier) }}
                            </div>
                            
                        </div>
                    <div class="form-group btn-right">
                        <button type="submit" class="btn btn-success"><i class="fa fa-upload" aria-hidden="true"></i>Mettre à jour</button>
                    </div>
                </div>
            </div>

        {{ form_end(edit_form) }}
        {% endblock %}
