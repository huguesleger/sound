{% extends ':back:templateAdmin.html.twig' %}

  {% block content %}
    <h1>Icon</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ icon.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ icon.nom }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('icon_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('icon_edit', { 'id': icon.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
