{% extends ':back:templateAdmin.html.twig' %}

  {% block content %}

    <h1>Section 1 creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('section_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
