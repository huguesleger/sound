{% trans_default_domain 'FOSUserBundle' %}
{% extends 'FOSUserBundle::layout.html.twig' %}
 {% block content %}
{% block fos_user_content %}
{#<form action="{{ path('fos_user_resetting_send_email') }}" method="POST" class="fos_user_resetting_request">
    <div>
        <label for="username">{{ 'resetting.request.username'|trans }}</label>
        <input type="text" id="username" name="username" required="required" />
    </div>
    <div>
        <input type="submit" value="{{ 'resetting.request.submit'|trans }}" />
    </div>
</form>#}


    <div id="login" class="animate form">
        <section class="login_content">
     <form action="{{ path('fos_user_resetting_send_email') }}" method="POST">
    <h1><img src="{{asset ('images/big-logo.png')}}" class="logo-connect"/></h1>
            <div>
              <input type="text" class="form-control" placeholder="{{ 'resetting.request.username'|trans }}" id="username" name="username" required="required" />
              <input type="submit" class="btn btn-dark col-md-12" value="{{ 'resetting.request.submit'|trans }}" />
            </div>
            <div class="clearfix"></div>
            <div class="separator">
              <div>
{#                <h1><i class="fa fa-paw" style="font-size: 26px;"></i> Gentelella Alela!</h1>#}
                <p><i class="fa fa-reply" aria-hidden="true"></i><a href="{{ path("home") }}">Retour au site</a></p>
              </div>
            </div>
          </form>
        <!-- form -->
        </section>
        <!-- content -->
      </div>
              
    {% endblock fos_user_content %}
 {% endblock %}