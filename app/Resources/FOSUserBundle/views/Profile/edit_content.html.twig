{% trans_default_domain 'FOSUserBundle' %}



   <div class="row">  
   <div class="col-md-6 col-xs-12">
              <div class="x_panel">
                <div class="x_title">
                    <h2>Edit profile</h2>
                  
                  <div class="clearfix"></div>
                </div>
                <div class="x_content">
{% block fos_user_content %}
{{ form_start(form, { 'action': path('fos_user_profile_edit'), 'attr': { 'class': 'fos_user_profile_edit' }}) }}

    {{ form_widget(form) }}
    
     <label for="fos_user_profile_form_avatar" class="input-label">
          <i class="fa fa-upload"></i>Select fichier</label>
          {{ form_row(form.avatar) }}
     
       
                {#<img id="selectImage" class="img-circle" src="{{asset('uploads/avatar/')~app.user.avatarPath}}" alt="your image" />#}
                <img class="default-avatar" id="selectImage" src="{{asset('uploads/avatar/')~app.user.avatarPath}}" alt="your image" />
                
                 {#{{ form_row(form.avatar) }}#}
     <p>taille maximum 128X128px</p>
     </div>
     
            </div>
   {#   <div>
        <input type="submit" value="{{ 'profile.edit.submit'|trans }}" />
    </div>
    #}
     <div class="form-group btn">
                        <button type="submit" class="btn btn-success">{{ 'profile.edit.submit'|trans }}</button>
                      </div>
                      
                      
                     
    
      </div>
    

   
{{ form_end(form) }}
  {% endblock fos_user_content %}

                </div>

             
    
                         
              



    

